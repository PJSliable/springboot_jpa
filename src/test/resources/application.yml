spring:
#Springboot에서는 별도의 설정이 없으면 기본적으로 메모리 모드로 돌림
#  datasource:
#    url: jdbc:h2:mem:test # DB 띄우기 없이 메모리로 DB 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
##      실행 시점에 table을 다 지우고 다시 생성
#      ddl-auto: create # create: entity drop - create - 실행, create-drop: create와 같고 마지막에 drop 추가
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
